//
//  FocusLiveActivityWidget.swift
//  cueLiveActivity
//
//  제거됨 — 집중 Live Activity는 AlarmKit이 구동하는 `FocusAlarmLiveActivityWidget`으로 대체됐다.
//  (옛 방식: 앱이 `Activity.update()`로 갱신 → 백그라운드에서 미반영. AlarmKit으로 전환.)
//  파일은 비워둔다(타깃 동기화 그룹이라 빈 파일은 무해).
//
